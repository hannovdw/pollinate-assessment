spring:
  application:
    name: challenge
  security:
    user:
      name: admin
      password: password
  h2:
    console:
      enabled: true
      path: /h2-console
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
